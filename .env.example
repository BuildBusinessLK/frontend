# Google AI API Configuration
# Get your API key from: https://ai.google.dev/
GOOGLE_AI_API_KEY=your_api_key_here

# Note: The PHP backend will use this environment variable
# If not set, it will use a hardcoded key as fallback (not recommended for production)
